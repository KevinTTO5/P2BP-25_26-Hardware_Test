# Enforce LF line endings for all files on checkout (Jetson runs Linux)
* text=auto eol=lf

# Explicitly binary — never mangle these
*.png binary
*.jpg binary
*.jpeg binary
*.pth binary
*.tar binary
*.weights binary
