################################################################################
# mv3dt-installer bundled smoke-test config
# (installer/plan/STEP-2-DEEPSTREAM-SDK.md section 7.3)
#
# BEST-EFFORT ARTIFACT: no DeepStream 9.1 install or GPU was available to
# validate this file in the environment it was authored in. It is written
# to be structurally correct per the DS 9.1 `deepstream-app` config file
# format, modeled on the layout of the DS 9.1 Quickstart sample
# (`source30_1080p_dec_infer-resnet_tiled_display.txt`) and targeting the
# sample ResNet/PeopleNet-compatible primary-gie path shipped under the
# standard DS 9.1 samples directory tree. Its only job is to prove the
# pipeline reaches PLAYING and runs decode + nvinfer (TensorRT) + tracker
# without error, headless (fakesink, no X/Wayland) -- not to validate
# detector accuracy. Operators should treat a failure here as a signal to
# re-check the DeepStream SDK install, not the model itself.
################################################################################

[application]
enable-perf-measurement=1
perf-measurement-interval-sec=5

[tiled-display]
enable=0
rows=1
columns=1
width=1280
height=720
gpu-id=0

[source0]
enable=1
type=3
uri=file:///opt/nvidia/deepstream/deepstream-9.1/samples/streams/sample_1080p_h264.mp4
num-sources=1
gpu-id=0
cudadec-memtype=0

[sink0]
enable=1
type=1
sync=0
source-id=0
gpu-id=0

[osd]
enable=0

[streammux]
gpu-id=0
batch-size=1
batched-push-timeout=40000
width=1920
height=1080
live-source=0

[primary-gie]
enable=1
gpu-id=0
batch-size=1
gie-unique-id=1
nvbuf-memory-type=0
config-file=/opt/nvidia/deepstream/deepstream-9.1/samples/configs/deepstream-app/config_infer_primary.txt

[tracker]
enable=1
tracker-width=640
tracker-height=384
ll-lib-file=/opt/nvidia/deepstream/deepstream-9.1/lib/libnvds_nvmultiobjecttracker.so
gpu-id=0

[tests]
file-loop=0
